# ============================================
# UŽIVATELÉ V ZABBIXU
# ============================================

# 1. API USER PRO GRAFANU (ZŮSTÁVÁ PŮVODNÍ)
- name: Create API user for Grafana integration
  community.zabbix.zabbix_user:
    username: "{{ zabbix_api_user }}"
    passwd: "{{ zabbix_api_user_password }}"
    role_name: "Super admin role"
    usrgrps:
      - "Zabbix administrators"
    state: present


- name: Create personal user with name, surname and school email
  community.zabbix.zabbix_user:
    username: "ondrej.jirotu"                # no trailing space
    name: "ondrej"
    surname: "jirout"
    passwd: "heslo123"
    role_name: "Super admin role"
    usrgrps:
      - "Zabbix administrators"
    user_medias:
      - mediatype: "Email"                   # must match an existing media type name in Zabbix
        sendto: "jirout.ondrej@student.sposdk.cz"
        active: true
        severity: 63                         # optional, default is all (63)
    state: present
  register: personal_user